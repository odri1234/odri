// src/common/enums/prediction-type.enum.ts

export enum PredictionType {
  BANDWIDTH_FORECAST = 'bandwidth-forecast',
  DEMAND_PREDICTION = 'demand-prediction',
  REVENUE_PREDICTION = 'revenue-prediction',
  USAGE_FORECAST = 'usage-forecast',
}
