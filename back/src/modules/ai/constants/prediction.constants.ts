// src/modules/ai/constants/prediction.constants.ts
export enum PredictionType {
  BANDWIDTH_FORECAST = 'bandwidth-forecast',
  DEMAND_PREDICTION = 'demand-prediction',
  USAGE_PREDICTION = 'usage-prediction'
}