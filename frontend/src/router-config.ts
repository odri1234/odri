// React Router configuration
import { UNSAFE_enhanceManualRouteObjects } from 'react-router-dom';

// Enable future flags to fix warnings
export const routerConfig = {
  future: {
    v7_relativeSplatPath: true,
  },
};